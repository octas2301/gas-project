---
description: 処理時間の全関数ばら撒き禁止。同様の指示は実装せず止めてアラートする
alwaysApply: true
---

# 処理時間の範囲（2026-08-16 確定）

人間が忘れても、次を指示したら **実装せず止めてアラート**する。

- 「B以外にも使えるように」
- 「全関数／全メニューに console.time」
- 「どこでも自動で計測」
- 「タイミングを全体にばら撒く」

## なぜ止めるか

全関数へ `console.time`／`logOpTiming_` を巻くと、実行ログ枠・シート `appendRow`・小関数の遅延が乗る。B 以外のメニューまで遅くなる。

## 正

- 共通は `logOpTiming_`（`コード.js`）だけ。`console.time` は使わない
- **配線してよいのは** B 統合の各 Step と Step5 の `geminiSearch` / `geminiVision` / `openaiVision` のみ
- 他ジョブはボトルネック **1か所** をユーザーが明示したときだけ、同じ関数を1行足す（承認後）
- 切戻し: `OP_TIMING_ENABLED=false`
- シート: `▼実行時間`

止めたときは「全関数計測は禁止。B＋Step5 API のみ。広げたい箇所を1つ指定してください」と返す。
