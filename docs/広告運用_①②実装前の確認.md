# ① ② レポート自動取得・フォルダ保存 — 実装前の確認

> **回答反映済み**  
> ご回答は **`広告運用_①②回答反映と仕様.md`** にまとめ、仕様を確定しました。Amazon を先に実装し、楽天は手動のまま。金曜 3:00 に「① ② → 分析＆バルク」の順で一括実行する形で進めます。

トリガー設定は完了しています。**① レポート自動取得** と **② 指定フォルダへの保存** の実装に進む前に、以下で希望と現状を教えてください。

---

## 1. Amazon Advertising API

- **Amazon 広告（スポンサー広告）の開発者登録・API 利用** はすでにしていますか？
  - している … そのまま「レポート取得 → 指定フォルダ保存」の設計に進めます。
  - していない … まず [Amazon Advertising API](https://advertising.amazon.com/API/docs) 側の登録・アプリ作成が必要です。登録予定か、別の方法（手動DLのまま等）を希望するか教えてください。
⇒希望します。リンクを開きましたが英語で大丈夫か不安です

- **取得したいレポート** は、現在手動で保存しているものと同じでよいですか？
  - 例：**SP 検索ワードレポート**（分析で使っているCSV）、**SP キャンペーンレポート**（ID参照用） など。必要なレポート名・種類を箇条書きでも構いません。
  ⇒同じでよいです
  　AmazonADSのメニューでは、1. カスタマイズしたスプレッドシートを作成しダウンロードする、という中で、スポンサーProduct広告の検索用語データ、と記載があります

---

## 2. 楽天広告 API

- 商品出品で **楽天API連携は済** とのことです。**広告（RPP 等）のレポート取得** は、その同じAPIでできますか？ それとも **広告用に別のAPI・申請** が必要でしょうか？
- 分からない場合 … 「未確認」とだけ書いていただければ、**Amazon 側の ① ② を先に実装**し、楽天は「手動でフォルダに保存」のまま後からAPI対応する形も取れます。
⇒未確認、以下がAPIの利用機能一覧です

利用機能一覧

※一度利用機能を設定した後は、全ての機能を解除することはできません。APIの機能が不要な場合はライセンスキーを停止してください。



LicenseManagementAPI(ライセンス管理API)

機能名	機能内容	利用状態
license.expiryDate.get	ライセンスキーの有効期限を取得することができます。Get the expiry date of the provided license.	利用中
 
R-CabinetAPI (CabinetAPI)

機能名	機能内容	利用状態
cabinet.file.delete	画像IDを指定して画像を削除フォルダに移動することができます。
This function enables the transfer of the image into Deletion folder by specifying the image ID.	利用中
cabinet.file.insert	画像ファイルを指定して画像を登録することができます。
This function enables the registration of the image by specifying the image file.	利用中
cabinet.file.update	画像IDを指定して画像情報を更新することができます。
This function enables the update of the image information by specifying the image ID.	利用中
cabinet.files.search	画像名を指定して画像を検索することができます。
This function enables the search of the image by specifying the image name.	利用中
cabinet.folder.files.get	指定したフォルダ内の画像一覧を取得することができます。
This function enables the acquisition of the list of the images stored in the specified folder.	利用中
cabinet.folder.insert	フォルダを作成することができます。
This function enables the creation of a folder.	利用中
cabinet.folders.get	フォルダの一覧を取得することができます。
This function enables the acquisition of the folder list.	利用中
cabinet.trashbox.file.revert	削除フォルダ内にある画像を指定したフォルダに戻すことができます。
This function enables the restoration of the image from Deletion folder to the specified folder.	利用中
cabinet.trashbox.files.get	削除フォルダ内にある画像の一覧を取得することができます。
This function enables the acquisition of the list of images stored in Deletion folder.	利用中
cabinet.usage.get	R-Cabinetの利用状況を取得することができます。
This function enables the acquisition of the usage status of R-Cabinet.	利用中
 
カテゴリAPI 2.0 (CategoryAPI 2.0)

機能名	機能内容	利用状態
category.category-set-lists.get	すべてのカテゴリセット情報を取得することができます。
retrieve all the category set information.	利用中
category.category-trees.get	カテゴリセットIDを指定しカテゴリツリー情報を取得することができます。
retrieve category tree information by categorySetId.	利用中
category.category-trees.upsert	カテゴリセットIDを指定し、カテゴリツリー情報を登録・更新・削除することができます。
register, update or delete category tree information by categorySetId.	利用中
category.item-mappings.delete	指定した商品管理番号を表示先カテゴリから削除することができます。
delete mapping information between item and category by manageNumber.	利用中
category.item-mappings.get	商品管理番号を指定し、カテゴリとの紐付き情報を取得することができます。
retrieve mapping information between item and category by manageNumber.	利用中
category.item-mappings.upsert	指定した商品管理番号の表示先カテゴリの登録や変更をすることができます。
register or update the display category of the specific item by manageNumber.	利用中
category.shop-categories.get	カテゴリIDを指定しカテゴリ情報を取得することができます。
retrieve category information by categoryId.
利用中
category.shop-categories.insert	カテゴリ情報を登録することができます。
register category information.	利用中
category.shop-categories.update	カテゴリIDを指定しカテゴリ情報を更新することができます。
update category information by categoryId.	利用中
 
ジャンル・商品属性情報検索API（NavigationAPI 2.0)

機能名	機能内容	利用状態
navigation_2.0.dictionary.get	指定したジャンルIDに紐づく商品属性情報と推奨値を取得することができます。
Get item attribute information and recommended values ​​associated with the specified genre ID	利用中
navigation_2.0.genres.attr.get	指定したジャンルIDに紐づく商品属性情報を取得することができます。
Get the item attribute information associated with the specified genre ID	利用中
navigation_2.0.genres.get	指定したジャンルIDに紐づくジャンル情報を取得することができます。
Get the genre information associated with the specified genre ID	利用中
navigation_2.0.version.get	ジャンル及び、商品属性情報が変更された最終日時を取得することができます。
Get the last date and time when the genre and item attribute information was changed	利用中
 
商品API 2.0 (ItemAPI 2.0) ※ご利用には商品一括登録へのお申込みが必要です

機能名	機能内容	利用状態
itemapi_2.0.inv-settings.get	商品管理番号を指定し、納期に関する設定などを取得することができます。
get delivery-related-setting information by manageNumber.	利用中
itemapi_2.0.inv-settings.update	商品管理番号を指定し、納期に関する設定などを更新することができます。
update delivery-related-setting information by manageNumber.	利用中
itemapi_2.0.items.bulk.get	この機能を利用すると、商品管理番号を指定し、最大で50件の商品情報を一括で取得することができます。
retrieve up to 50 items at once by using manageNumber	利用中
itemapi_2.0.items.delete	商品管理番号を指定し、商品情報を削除することができます。
delete item information by manageNumber	利用中
itemapi_2.0.items.get	商品管理番号を指定し、商品情報を取得することができます。
retrieve item information by manageNumber	利用中
itemapi_2.0.items.patch	商品管理番号を指定し、商品情報の部分更新をすることができです
partially update item information by manageNumber	利用中
itemapi_2.0.items.search	指定した条件から商品情報を検索することができます。
search item information by specified search conditions.	利用中
itemapi_2.0.items.upsert	商品管理番号を指定し、商品情報の登録・全項目の更新をすることができます。
create item, update all item information by manageNumber	利用中
 
在庫API 2.0 (InventoryAPI 2.0)

機能名	機能内容	利用状態
inventories.bulk.get	商品管理番号とSKU管理番号を指定し、在庫数を一括で取得することができます。
retrieve item quantity in bulk by manageNumber and variantId.
利用中
inventories.bulk.get.range	在庫数の上限下限を指定し、商品管理番号、SKU管理番号、在庫数、登録日時、更新日時を取得することができます.
retrieve manageNumber, variantId, item quantity and date information in bulk by specifying the range of the quantity.	利用中
inventories.bulk.upsert	商品管理番号とSKU管理番号を指定し、在庫数を一括で登録・更新することができます。
register, update item quantity in bulk by manageNumber and variantId.	利用中
inventories.variants.delete	商品管理番号とSKU管理番号を指定し、在庫情報を削除することができます。
delete item quantity by manageNumber and variantId.	利用中
inventories.variants.get	商品管理番号とSKU管理番号を指定し、在庫数を取得することができます。
retrieve item quantity by manageNumber and variantId.	利用中
inventories.variants.upsert	商品管理番号とSKU管理番号を指定し、在庫数を登録・更新することができます。
register, update item quantity by manageNumber and variantId.	利用中
 
在庫API 2.1 (InventoryAPI 2.1)

機能名	機能内容	利用状態
inventories.bulk.get.range_2.1	在庫数の上限下限を指定し、商品管理番号、SKU管理番号、在庫数、出荷リードタイム、出荷元住所、登録日時、更新日時を取得することができます。
retrieve manageNumber, variantId, item quantity, shipping lead time, ship-from address and date information in bulk by specifying the range of the quantity.	利用中
inventories.bulk.get_2.1	商品管理番号とSKU管理番号を指定し、在庫数、出荷リードタイム、出荷元住所関連の情報を一括で取得することができます。
retrieve item quantity, shipping lead time, ship-from address in bulk by manageNumber and variantId.	利用中
inventories.bulk.upsert_2.1	商品管理番号とSKU管理番号を指定し、在庫数、出荷リードタイム、出荷元住所関連の情報を一括で登録・更新することができます。
register, update item quantity, shipping lead time, ship-from address in bulk by manageNumber and variantId.	利用中
inventories.variant-lists.get	この機能を利用すると、商品管理番号を指定し、在庫情報として保持している全てのSKU管理番号を取得することができます。
Get the list of variant IDs for inventories by specifying manage number	利用中
inventories.variants.delete_2.1	商品管理番号とSKU管理番号を指定し、在庫情報を削除することができます。
delete item quantity by manageNumber and variantId.	利用中
inventories.variants.get_2.1	商品管理番号とSKU管理番号を指定し、在庫数、出荷リードタイム、出荷元住所関連の情報を取得することができます。
retrieve item quantity, shipping lead time, ship-from address by manageNumber and variantId.	利用中
inventories.variants.upsert_2.1	商品管理番号とSKU管理番号を指定し、在庫数、出荷リードタイム、出荷元住所関連の情報を登録・更新することができます。
register, update item quantity, shipping lead time, ship-from address by manageNumber and variantId.	利用中
---

## 3. 保存先フォルダ

- 現在の **AMZ_FOLDER_ID**（Amazon用）と **RAK_FOLDER_ID**（楽天用）のフォルダに、**① ② で取得したCSVをそのまま保存** してよいですか？
  - よい … そのまま「レポート取得 → そのフォルダに保存」で設計します。
  - 別フォルダにしたい … 「レポート用」など、保存先の希望（例：同じフォルダ内のサブフォルダ名）を教えてください。
よいです
---

## 4. 実行タイミング

- **隔週金曜 3:00** のトリガーで「分析＆バルク生成」は設定済みです。
- **① ②（レポート取得・保存）** は、どのタイミングで動かしたいですか？
  - **隔週金曜の 3:00 より前**（例：木曜 23:00 にレポート取得 → 金曜 3:00 に分析＆バルク） … その場合、① ② 用のトリガーを「木曜 23:00」など別で設定する形になります。
  - **隔週金曜 3:00 のトリガー内で、分析の前に実行** … 金曜 3:00 に「① ② → 分析＆バルク」の順で一括実行する形です。
  - **手動のみ** … メニューから「レポート取得＆保存」を実行。自動は後回し。
- 希望に近いものを 1 つ選ぶか、文言で指定してください。
金曜 3:00 に「① ② → 分析＆バルク」の順で一括実行です
---

## 5. その他

- 上記以外で、**レポートの種類・取得頻度・ファイル名のルール** など決めておきたいことがあれば書いてください。
　今はありません

---

ご回答をいただいた内容で、① ② の具体的な仕様（どのAPI・どのレポートを、いつ、どのフォルダに保存するか）をまとめ、実装案を出します。
