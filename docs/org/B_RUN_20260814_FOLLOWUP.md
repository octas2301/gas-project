# B完走フォローアップ（2026-08-14）

**文書種別**: 問題メモ＋改修要件（問題は消さない）  
**日付**: 2026-08-14  
**親**: [CURRENT_PHASE.md](../CURRENT_PHASE.md)／[B_HARD_DEATH_SCOPE_REQUIREMENTS.md](B_HARD_DEATH_SCOPE_REQUIREMENTS.md)

完走観測（サマリ 11:13〜14:07、キュー連鎖）と社長確認を正とする。  
**⑥ N列はコード改修しない。** 人間がメニューA（Keepa取得）→B 再実行。同じ結果なら原因を再調査。

---

## クローズ（改修しない）

| ID | 内容 |
|----|------|
| ⑦ | 8:08 `watchdog_stale_resume` は昨夜別件 |
| ⑧ | Yahoo ブランド `38074` は仕様（石原水産未登録） |
| ⑨ | 行121〜は旧データ |
| ⑩ | 親の販売価格空・子に価格は正しい |
| ⑪ | `deleteBIntegratedState` → 連鎖 `resetInserted` は正常 |

---

## 改修対象

### ① 行高さ（見本子行・CLIP）

- 旧: 親だけ 63px。DF WRAP が勝ち、22行目のように膨らむ。
- **今**: ヘッダー直後の子行高さを当Bの全行に CLIP＋固定（15–40px以外は21）。

### ② FBAティア

- N列 `ASINコード` は⑥により **競合ASIN（相乗り先）**。P1b は N列を先に使い、空なら競合店ASIN。自己発寸法なしは継続。

### ⑦ CT/CU/CV 式

- 選定不可時の `setValue('')` が CV 等の式を潰していた（CV99 数式バー空で確認）。
- **対策**: 空にする代わりに 2行目式をコピー。CP 空なら表示は空のまま。後からサイズを入れると再計算する。

### ⑧ LJ 保存方法改行

- **対策**: B Step **6.05**（Step6 直後追加）で 15-⑭ 相当を quiet 実行。

### ⑨ 楽天ヒット

- **対策**: JAN 0件→メーカー名＋商品名→**商品名のみ**。JAN keyword ヒット時は名前閾値 30。タイトルの **N袋** をセット数としてパース。

### ⑩ Yahoo ヒット

- サイト検索は `query=JAN`。API は `jan_code` のみだった。
- **対策**: `jan_code` 0件なら `query=JAN`、それでも 0 ならメーカー＋名。袋パースも共通。

### ③ ネコポス埋め

- 寸法なし経路が最安ネコポスを埋めていた。
- **対策**: 寸法なし／選定不可はサイズ・箱・送料を空。`利益確保不可_理由` に `寸法なし` または `サイズ選定不可`。`送料利益確保不可` も立てる。
- B-② は現状維持（人間がサイズ確定後。送料フェーズなし）。

### ③b size=170・送料2100

- 3D first-fit のサイズ名と、2行目送料式の照合ずれが疑わしい。
- **対策**: 自己発送表に無い／式が解けないサイズ名は書かない（空＋理由）。原因ログを残す。

### ④ DE/ED/EX の色

- CPO が緑／ピンク（役割色）で⑧f の黄を上書きしていた。
- **対策**: **CPO は色を塗らない。** 利益不足の濃赤は、サイズ特定の精度が上がってから再開（本改修では塗らない）。

### ⑤ GB〜GD 赤（KW 重複）

- 7.6 は動くが、最終名との重複を消していなかった（`seenMall` 分裂＋特徴用途 `keep_existing`）。
- **対策**: seen は左→右 1本。  
  `メーカー名・商品名ベース → メインKW → CTR → 特徴 → 用途 → 補足KW → 検索KW`  
  前セル既出トークンは後ろのセルから落とす。商品名ベース本体は削らない。

### ⑥ N列 ASIN（コード改修なし・再テスト待ち）

- 公式カタログでも、実装は親の競合店ASIN＋評価文字 `◎`＋ブランド＝メーカー。
- A 未実行だと Keepaログ経路は空振り。貼付◎が無いと同じ。
- **次**: 人間が A → B。同じなら原因再調査。

### ⑥再 AI情報取得data の Gemini 消失

- Step5 が全行 `clearContent` していた。ループ内は既に当B以外スキップ。
- **対策**: clear も当B行（`queuedAiRows` / `insertedProducts`）だけ。再開 state に行集合を持つ。

---

## 実装順

1. ③ 空欄＋理由（③b 含む）
2. ④ CPO 非塗色
3. ⑥再 clear 範囲
4. ② FBA を B に載せる
5. ⑤ 重複ドロップ
6. ① 高さ固定

⑥ N は上記完了後の A→B 結果待ち。コード改修は 2026-08-14 実装済（clasp push は人間）。

## 復元

Git revert。要 clasp push。楽天CSV・Yahoo.js・B Step **順序の入替**（追加は 6.5 の直後のみ）はしない。
