# B / Z — N列ASIN自動（⑥）要件

**文書種別**: 要件定義＋実装  
**日付**: 2026-08-12  
**状態**: **実装済**（要 `clasp push`）  
**親**: [CURRENT_PHASE.md](../CURRENT_PHASE.md)／[AGENT_HANDOVER.md](../AGENT_HANDOVER.md) §8  
**手順**: [B_ASIN_N_AUTO_FILL_HUMAN_RUN.md](B_ASIN_N_AUTO_FILL_HUMAN_RUN.md)  
**コード**: `コード.js`（`fillAsinNFromOfficialCompetitor_` ほか）  
**ゴール一文**: Keepa評価◎の競合ASINかつブランド＝メーカーのとき、相乗り用 **N列`ASINコード`を空欄のみ自動記入**し、黄セル＋要確認で人が最終チェックする。

---

## 1. 確定ルール（ロック）

| 項目 | 内容 |
|------|------|
| 条件 | **◎かつブランド＝メーカー** |
| ◎の正 | 同一JANの Keepa取得_ログ最新回の評価◎ **または** `ASIN貼り付け（Keepa用）` ブロックの評価◎ |
| ブランド＝メーカー | マスタ `メーカー名` と `▼マスタ(ブランド)`（なければ `ブランド`）を空白除去・小文字化して一致 |
| 書き元 | 親行の `競合店ASINコード`（先頭有効ASIN）。無ければ `競合AmazonページURL` から抽出 |
| 書き先 | `ASINコード`（N列）。**既存値は上書きしない** |
| 要確認 | セル背景 `#fff2cc` ＋ note ＋ 列 `要確認_ASINN`＝要確認 |
| 対象行 | 出品CK（レ点）付き**親行**（メニュー8と同じ収集） |

---

## 2. 入口

| 入口 | ゲート |
|------|--------|
| B → **Step6.5**（Step6同期の直後） | `B_ASIN_N_AUTO_FILL_ENABLED` **未設定＝ON** |
| Z → **15-⑮** | 同上（オフ時はアラート） |

---

## 3. 作らないもの

- 既存N列の上書き
- ◎無し／ブランド≠メーカーでの自動記入
- 楽天CSV・Yahoo.js・相乗り PUT 本体の変更
- ④サイズ/FBA・⑤競合精度・メーカー品番B載せ（別タスク）

---

## 4. 復元

- Property `B_ASIN_N_AUTO_FILL_ENABLED=false`
- Git revert
- 記入済みNはシート版履歴または手削除（黄セルを目安）

---

## 5. 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-08-12 | v1 実装: Step6.5／15-⑮／黄セル＋要確認_ASINN |
