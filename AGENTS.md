# AGENTS.md — gas-project エージェント運用ガイド（v2）

**対象**: 本リポジトリ（gas-project）で作業する **すべてのエージェント**（ローカルの Cursor Agent／Cloud Agent／その他）。
**最終更新**: 2026-07-11
**目的**: レビュー観点・検収基準・クラウド専用注意を1か所に集約し、**安全に・壊さずに・戻せる形で**変更を行うための共通ルールとする。

> このファイルはリポジトリ直下に置き、全エージェントが最初に読む。詳細な業務・実装仕様は `docs/` を正とする（重複記載はせず参照する）。

---

## 0. まず読む（インプット順）

1. **`docs/CURRENT_PHASE.md`** … いまの開発フォーカス・スコープ外を把握する。
2. **`docs/AGENT_HANDOVER.md` §2** … 全エージェント必読の資料一覧（FLOW_AND_PRIORITY / REQUIREMENTS / MASTER_LINKAGE_TASKS / PROJECT_OVERVIEW ほか）。
3. **`HANDOVER.md`** … 楽天・Yahoo 出品の実装詳細（シート名・列名・API仕様・コード行番号）。

作業で確定した仕様・前提は、**必ず該当 docs に反映**する（`docs/AGENT_HANDOVER.md` §4 の双方向インプット必須ルール）。

---

## 1. プロジェクト概要（30秒）

- **目的**: Amazon・楽天・Yahoo! での売上を **利益が出る状態で拡大** するため、リサーチ〜商品情報・価格・セット構成〜出品を **スプレッドシート＋GAS** で自動化する。
- **実行基盤**: Google Apps Script（GAS）。ローカルは **clasp** で同期。
- **主要ファイル**:
  - `コード.js` … メニュー（onOpen）・B統合実行・価格/CPO・リサーチ・楽天CSV出力・AIデータ同期・Webアプリ削除API等。**巨大な単一ファイル（約1.1MB）**。
  - `Yahoo.js` … Yahoo! 出品専用（`YahooDataBuilder` / `YahooApiClient` / 画像API・editItem・setStock・submitItem）。
  - `appsscript.json` … GAS マニフェスト。
- **主要シート**: `▼商品マスタ(人間作業用)` / `AI情報取得data` / `00_設定マスタ` / `▼設定(Yahooマッピング)` / `▼ログ(システム用)`。
- **`.clasp.json` の `scriptId` を GAS 連携の正**とする（`HANDOVER.md §6` に古い scriptId 記載が残るが、実体は `.clasp.json` を優先）。

---

## 2. 絶対に守る前提（Sanctuary / 破壊禁止）

1. **楽天の既存ロジックは聖域**。`generateRakutenCSV` を中心とする楽天CSV生成・FTP(MAKE)連携・反映確認は **完成品**。原則 **変更・干渉禁止**。触れる必要があるときは事前に理由を提示し承認を得る。
2. **Yahoo! は楽天と分離**して管理する（`Yahoo.js`）。責務を混ぜない。
3. **B統合実行の Step 境界は `コード.js` の `B_INTEGRATED_STEP_FUNCTIONS` を正**とする（`docs/AGENT_HANDOVER.md §8` / `HANDOVER.md §8.7`）。勝手に順序・境界を変えない。
4. **シークレットをコード/コミットに入れない**。APIキーは **Script Properties**（`OPENAI_API_KEY` / `GEMINI_API_KEY` / `RAKUTEN_LICENSE_KEY` / `RAKUTEN_SERVICE_SECRET` など）から参照のみ。`HANDOVER.md §7` 参照。

---

## 3. 変更の進め方（承認ルール）

`.cursor/rules` および運用ルールに従う。要約:

### 3.1 そのまま実行してよい（軽微）
- **単一ファイル かつ 約50行未満** の変更。

### 3.2 実装前に必ず承認を得る
提示するもの: **変更予定ファイル一覧 / 変更概要 / 想定リスク**。

- 一般: 複数ファイルに影響／約200行超／**新規ファイル作成**／依存関係の変更。
- **ECデータ操作に関する重要変更（規模を問わず必ず事前承認）**:
  - CSV/TSV/スプレッドシート全体の書き換え
  - SKU / JAN / 商品ID の一括更新
  - 価格・在庫・商品情報の一括更新ロジック
  - **全件ループ（全商品処理）** を含む変更
  - **外部EC API への書き込み（POST/PUT/PATCH）**
  - **バリエーション構造** に影響する変更
  - 商品データのマッピング仕様変更

---

## 4. 実装時の必須3点セット（承認に自動的に含まれる）

`docs/AGENT_HANDOVER.md §9 / §9.1` を正とする。実装するときは毎回セットで実施:

1. **要件定義（docs）更新** … 確定仕様・列名・タスクIDを該当 docs に追記／更新し、`AGENT_HANDOVER.md §6` に1行サマリ。フェーズが変われば `CURRENT_PHASE.md` も更新。
2. **調査用ログ** … 新規・変更した分岐に、判断理由・入力サイズ・エラー要約を `Logger.log`。統合/単独メニューとも **`runId` / `stepIndex` / `stepName` / `functionName` / `state`（PENDING/RUNNING/DONE/FAILED/RETRYING）** のログ標準に沿う。
3. **復元手段** … **Git は小さなコミット・revert 可能な単位**（1タスク1ブランチ）。新規ロジックは可能なら **Script Properties のブール/数値トグル**で on/off・閾値切替（既存 `getBoolScriptProperty_` 等と同パターン）。必要に応じ `docs/CHANGE_LEDGER.md` に「対象関数・目的・戻し方(commit hash)」を記録。

---

## 5. レビュー観点（コードレビュー時のチェックリスト）

### 5.1 GAS 実行環境
- [ ] **実行制限**を意識。**本PJの日中出品ジョブ**は課金枠30分前提で **約25分で中断→トリガー再開**（[org/AI_APPROVAL_MATRIX.md](docs/org/AI_APPROVAL_MATRIX.md) §5）。無料枠向けの5分30秒／6分は **本ジョブの既定ではない**（他スクリプト・他環境では従来どおり注意）。
- [ ] `SpreadsheetApp` の I/O を無駄にループ内で叩いていないか（バッチ読み書きになっているか）。
- [ ] `getRange(row, col, numRows, numCols)` の **第3・4引数は「行数・列数」**。件数と取り違えて `setValues` 範囲不一致を起こしていないか（既知の警告例あり）。

### 5.2 マスタ・親子・レ点
- [ ] **親行判定＝「子SKU空 かつ 出品CK」**、子行は親SKU一致で紐づく、という前提を壊していないか。
- [ ] **チェックボックス判定は boolean `true` と 文字列 `"TRUE"` の両対応**（`Yahoo.js` の `yahooMasterCheckboxIsTrue_` 相当）。`=== true` だけに戻していないか。
- [ ] **Yahoo! の出品対象は「子SKUのレ点」のみ**。「親だけレ点で全子出品」を実装していないか（禁止仕様。`HANDOVER.md §5.2.1`）。
- [ ] JAN 参照前提の式（親SKU/子SKU/AK 等）で **JAN 欠落時に空になる**波及を考慮しているか。

### 5.3 Yahoo! API（`Yahoo.js`）
- [ ] `seller_id` の場所: **画像API=URLクエリ / 商品・在庫API=ボディ**（取り違えると `px-04001` / `400`）。
- [ ] パラメータ名: 商品名は `name`、価格は `price`（`item_name`/`item_price` ではない）。
- [ ] `taxable` は **数値 `1/0`**（文字列 `"1"` 不可）。`ship_weight` は空なら **送らない**。
- [ ] `grouping_id` は **半角英数字・ハイフンにサニタイズ**して送信。
- [ ] カテゴリ `path` 等は **API向けに正規化**（`normalizeYahooItemPathForApi_` 相当）してから送っているか。
- [ ] `name` は全角75文字、`caption` 5000（HTML可）、`explanation` 500（HTML不可）、`headline` 30 の制約を守るか。
- [ ] 新規出品は `submitItem` が使えず **手動反映**（`it-07004` は成功扱い）という仕様を壊していないか。

### 5.4 文字コード・出力
- [ ] 楽天CSV は **Shift-JIS**。エンコードを壊す変更をしていないか（Base64直送で壊れた既往あり／Drive経由で解決済み）。
- [ ] 改行は Windows 環境で `LF↔CRLF` の警告が出るが、**コード内容の意図しない全書き換え**になっていないか（差分が巨大化していないか）。

### 5.5 安全性
- [ ] 楽天聖域・B統合境界・シークレット非混入（§2）を満たすか。
- [ ] EC重要変更（§3.2）に該当する場合、**承認済み**か。
- [ ] 必須3点セット（§4）を満たすか。

---

## 6. 検収基準（この変更は「完成」と言えるか）

以下を **すべて満たしたときに完了**とする。

- [ ] 目的の挙動を満たし、**楽天聖域・Yahoo分離・B統合境界を壊していない**。
- [ ] §5 のレビュー観点に反していない（該当項目を確認済み）。
- [ ] **シークレット・巨大バイナリを含まない**（§7）。
- [ ] EC重要変更なら **事前承認済み**、かつ影響範囲がドキュメント化されている。
- [ ] **必須3点セット（docs更新・調査ログ・復元手段）** を実施済み。
- [ ] 変更は **小さなコミット**で、`git revert` で戻せる。
- [ ] （実機反映が必要な場合）**ローカルで `clasp push` → GAS 実行 → ログ確認**の手順が明記され、人間が実行できる状態。※ Cloud 上では実機反映しない（§8）。

---

## 7. リポジトリ衛生（コミットしてよいもの/ダメなもの）

- **コミット対象（コード）**: `コード.js` / `Yahoo.js` / `appsscript.json` / `docs/*.md` / 設定系（`.clasp.json`・`.claspignore`・`.gitignore` 等）。
- **コミット禁止 / 追跡しない**:
  - **APIキー・トークン・認証ファイル**（`~/.clasprc.json` 等）。
  - **巨大バイナリ**（`参考資料.../*.mp4` `*.pdf` `*.html` 等）。GitHub は **100MB超を拒否**（過去に push 拒否の実績あり）。`.gitignore` で除外し、必要な素材はローカル/Drive で管理する。
  - 一時ファイル（`*_diff.txt`・`*.local_backup` 等の作業用出力）。
- **`clasp push` に載せない**もの（`.claspignore`）: `docs/**`・`*.md`・`export-tax-list/**` 等。**このAGENTS.mdもGASには送られない**（`*.md` 除外）。

---

## 8. クラウド専用注意（Cloud Agent 向け）

Cloud Agent は **リポジトリのコードだけがある隔離VM**で動く前提。次を厳守する。

### 8.1 Cloud では「できないこと」＝やらない
- **`clasp pull` / `clasp push` は実行しない**（Google/GAS 認証が無い。実機反映は必ずローカルで人間が行う）。
- **スプレッドシート等の本番データにアクセスしない**（Cloud からは到達不能。実データ前提の検証を「できたつもり」で進めない）。
- **外部EC API（Amazon SP-API / 楽天RMS / Yahoo API 等）への書き込みをしない**。読み取り検証が必要でも、認証・本番影響があるため **原則コード上の静的検討に留める**。
- **巨大バイナリを追加しない**（§7）。`git add .` を安易に使わず、**変更ファイルを明示指定**する。

### 8.2 Cloud で「やること」
- **コードレビュー・静的解析・設計提案・docs 整備**を中心にする。
- 変更は **差分（diff）と根拠**を明確にし、**実機での検証手順（ローカルで clasp push → 実行 → ログ）** を成果物に添える。
- レビュー時は **本ファイル §5（観点）・§6（検収）・§2（聖域）** を必ず適用する。

### 8.3 環境設定
- Cloud 実行の環境定義が必要な場合は `.cursor/environment.json`（install/start コマンド・Dockerfile 参照）を用意する。**本番シークレットは環境に置かない**（レビュー用途では不要）。
- 支出上限（Spend Limit）はユーザー側で設定済み。長時間・広範囲の探索は避け、目的に絞る。

---

## 9. ローカルでの実機反映フロー（人間 or ローカルAgent）

1. `clasp pull` で GAS 最新を取得（トークン期限切れ `invalid_rapt` 時は `clasp login` で再認証）。
2. 変更を実装（§3〜§5）。
3. `clasp push` で GAS へ反映。
4. スプレッドシートのメニューから該当機能を実行し、**`▼ログ(システム用)` 等でログ確認**（`runId`→失敗Step特定→Step別ログ）。
5. 問題なければ **対象ファイルだけを `git add`** → 小さなコミット → push。

> `コード.js` は巨大な単一ファイル。**全文書き換え型の編集は避け**、対象関数の周辺だけを最小差分で変更する。

---

## 10. 今後のエージェント化（設計中・参考）

将来的に、以下の「自律ループ型エージェント」を検討中（要件は別 docs で確定させる。未確定のまま実装しない）:

- **カテゴリ決めエージェント**: Amazon/楽天/Yahoo! の公式マスタ検索 → マスタ書き込みを道具（tool）として与え、競合カテゴリ投票（JAN優先・商品名フォールバック）を標準入力に、正しいカテゴリID確定と「要確認」フラグ付けまでを自律実行。
  - 楽天: **RWSジャンル検索で候補選定 → RMS NavigationAPI 2.0 で整合確認＋商品属性取得**（属性不一致エラー対策）。
  - 通知: 赤セル＋「要確認」書き込みを確定とし、A＋B＋Cの通知を併用。

確定した設計・道具・終了条件は、`docs/` に要件定義として追加してから実装する。

---

## 11. 参照
- 実装詳細（楽天/Yahoo・シート・列・行番号）: `HANDOVER.md`
- 全体像・6領域: `docs/PROJECT_OVERVIEW.md`
- 現在フェーズ: `docs/CURRENT_PHASE.md`
- エージェント共通・双方向インプット・必須3点セット: `docs/AGENT_HANDOVER.md`
- フロー/優先度: `docs/FLOW_AND_PRIORITY.md`
- 価格・CPO・B統合: `docs/PRICING_V1_REQUIREMENTS.md`・`docs/CPO_PRICING.md`・`AGENT_HANDOVER.md §8`
- AIルーティング(Gemini/OpenAI): `docs/AI_ROUTING_GEMINI_OPENAI.md`
